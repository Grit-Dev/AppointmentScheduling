<div class="row">
    <div class="container">
        @if(User.IsInRole(AppointmentScheduling.Helpers.Helper.Admin))
        {
            <div class="form-control row col-2">
                <label>Select Doctor</label>
                @Html.DropDownList("doctorId", new SelectList(ViewBag.DoctorList, "Id","Name"), new {@class ="form-control select2"})
            </div>

        }
        <div id="calendar">

        </div>
    </div>
</div>